# FlipAnimation
This is a simple animation in JavaScript that flips from dark side lightsaber to light side lightsaber around the z-axis when you hover over the panel.

## Installation
To install, create a folder to house the project files, then run the HTML file. Hover over the lightsaber and it will rotate along the z-axis responsively relative to the position of your cursor.
